<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página de Inicio</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <style>
        body {
            background-color: #343a40;
            color: #ffffff;
        }

        .container {
            max-width: 600px;
            margin-top: 50px;
        }

        h2 {
            color: #17a2b8;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            padding: 15px 30px;
            font-size: 1.5rem;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }

        .btn-success {
            background-color: #28a745;
            border-color: #28a745;
            padding: 15px 30px;
            font-size: 1.5rem;
        }

        .btn-success:hover {
            background-color: #218838;
            border-color: #218838;
        }
    </style>
</head>
<body>

<div class="container">
    <h2 class="text-center">Bienvenido E-commerce.net</h2>

    <div class="text-center">
        <a href="@Url.Action("InicioSesion", "InicioSesion")" class="btn btn-primary btn-lg">Iniciar Sesión</a>
    </div>

    <div class="text-center mt-3">
        <a href="@Url.Action("Registro", "Registro")" class="btn btn-success btn-lg">Registrarse</a>
    </div>
</div>

</body>
</html>
